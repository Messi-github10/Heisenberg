#version 450

// Adapted from AOCE (MIT); see AOCE_LICENSE.txt.
layout (local_size_x = 16, local_size_y = 16) in;

layout (binding = 0, rgba16f) uniform readonly image2D inTex;
layout (binding = 1, rgba16f) uniform writeonly image2D outTex;

layout (std140, binding = 2) uniform GammaParams {
    float gamma;
} params;

void main() {
    ivec2 pixel = ivec2(gl_GlobalInvocationID.xy);
    ivec2 size = imageSize(inTex);
    if (pixel.x >= size.x || pixel.y >= size.y) return;

    vec4 color = imageLoad(inTex, pixel);
    vec3 corrected = pow(max(color.rgb, vec3(0.0)),
                         vec3(max(params.gamma, 0.0001)));
    imageStore(outTex, pixel, vec4(corrected, color.a));
}
